import React from 'react';
import ReactDOM from 'react-dom';

import './index.sass';
import Toogle from './Pages/Toggle';

ReactDOM.render(
  <React.StrictMode>
    <Toogle />
  </React.StrictMode>,
  document.getElementById('root')
);
